# Java Backend Project

Це серверна частина додатку на Java (Spring Boot). Нижче наведено інструкцію для локального запуску фронтенд-розробником.

## 🛠 Попередні вимоги
Перед початком переконайтеся, що у вас встановлені:
1. **JDK 17** (або 21, залежно від версії проекту).
2. **Docker & Docker Compose** (для швидкого запуску бази даних).

## 🚀 Швидкий старт (Локальний запуск)

### 1. Клонування репозиторію
```bash
git clone <посилання-на-ваш-репозиторій>
cd <назва-папки-проекту>

2 Запуск бази даних

У корені проекту вже є файл docker-compose.yml. Щоб підняти базу даних (PostgreSQL), виконайте:

Bash
docker-compose up -d
База буде доступна на порту 5432.

3. Запуск Java сервера

Використовуйте Maven Wrapper, щоб не встановлювати Maven вручну:

macOS / Linux:

Bash
chmod +x mvnw
./mvnw spring-boot:run

Сервер зазвичай запускається на порту http://localhost:8080.

API Документація
Після запуску сервера ви можете переглянути доступні ендпоінти (Swagger): 👉 http://localhost:8080/swagger-ui.html